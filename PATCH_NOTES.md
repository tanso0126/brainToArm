# 변경 기록

이 문서는 최초 Git 기준점 이후 적용한 패치를 순서대로 기록합니다.
각 패치의 정확한 줄 단위 변경은 Git 커밋에 보존되어 있습니다. 예전 영문
원문 전체는 `physical-macos-baseline-2026-07-30` 태그와 그 이전 커밋에서
확인할 수 있으며, 현재 문서는 친구가 읽기 쉽도록 한국어 요약으로
정리했습니다.

> 과거 작업 도중 패치 41–43 번호가 서로 다른 작업에 중복 사용됐고 패치
> 56 번호가 빠졌습니다. 역사를 임의로 고치지 않기 위해 당시 번호를 그대로
> 유지합니다.

## 최초 기준점 — `e231dce`

- `main` 브랜치에 당시 구현 상태를 그대로 저장했습니다.
- 파이프라인 검사, 설정 검사, Python 컴파일, 자동 오케스트레이터 검사가
  통과한 상태를 기준으로 삼았습니다.

## 패치 1–15 — 저장소, 뇌파 통신, 실시간 화면

1. 생성 파일과 뇌파 기록을 Git에서 제외하고 변경 기록을 만들었습니다.
2. 행동 시작 시점에 맞춘 ErrP 수집과 자료 부족 시 안전 중단을 구현했습니다.
3. 실물/모의 로봇 모드를 분리하고 시리얼 명령 검사를 엄격하게 했습니다.
4. 시각 서보 보정이 실제 집기 동작까지 이어지게 했습니다.
5. 작업 범위 ErrP 거부와 도달 가능 범위 검사를 추가했습니다.
6. 재현 가능한 ErrP 학습 자료와 설정에 묶인 모델을 만들었습니다.
7. USB 장치를 명확히 구분하고 LXSDF 채널 배치를 엄격히 검사했습니다.
8. 안전한 시작·종료, 오류에 강한 검사, 제한된 수동 조작을 추가했습니다.
9. 실물 장치 보정이 완료돼야 실행되는 조건을 추가했습니다.
10. 보정 결과를 과장하지 않게 표시하고 Windows 연결 다리의 범위를
    제한했습니다.
11. 변경 기록을 시간순으로 정리하고 최종 감사를 수행했습니다.
12. macOS에서 PolyG-I HID를 직접 수신하게 했습니다.
13. PolyG-I 8채널을 보는 로컬 실시간 화면을 만들었습니다.
14. 실시간 파형이 끊겨 보이지 않도록 부드러운 재생을 적용했습니다.
15. D1WD10 수신, ADC 환산, 고정 Y축 표시를 바로잡았습니다.

## 패치 16–28 — 실물 로봇팔, 펌웨어, 초기 카메라

16. Arduino Uno 실물을 연결하고 안전한 수동 제어 인수인계를 만들었습니다.
17. 고정 평면에서 영상으로 물체를 집고 들어 옮겨 놓는 흐름을 만들었습니다.
18. 4번과 6번 서보의 기본 자세를 180°로 바꿨습니다.
19. 1번 베이스 회전 제어를 복구했습니다.
20. HOME 값을 한 곳에서 관리하고 6번 서보 기본값을 0°로 바꿨습니다.
21. 유선 OV2640 USB 진단을 추가했습니다.
22. ESP32와 Uno 시리얼 장치를 동시에 구분했습니다.
23. 유선 OV2640 영상 수신 경로를 복구했습니다.
24. OV2640 비트 순서, 색상, 영상 품질을 보정했습니다.
25. Uno HOME 펌웨어를 안전하게 동기화했습니다.
26. 쓰지 않는 모터 칸을 없애고 실제 6개 서보 배선으로 옮겼습니다.
27. 5번 집게와 6번 집게 방향 회전의 의미를 서로 바꿔 바로잡았습니다.
28. 연속 TAR 인지 부하와 ErrP 기반 가변 자율성 배분을 추가했습니다.

## 패치 29–40 — 손목 웹캠과 실물 바닥 접근

29. 손목 웹캠을 지정하고 파란색/빨간색 집게 형상을 인식했습니다.
30. 실제 집게 화면을 측정하고 충돌 검사된 전체 화면 탐색을 만들었습니다.
31. 당시 대회용 선명한 노란색 물체에 목표를 고정했습니다.
32. 위로 든 최신 탐색 HOME과 관절 제한을 동기화했습니다.
33. 고정 장착된 집게 형상과 공용 실시간 미리보기를 만들었습니다.
34. 집게가 180°에서 완전히 닫히는 것을 확인했습니다.
35. 표시용 그림이 다시 인식 입력으로 들어가는 문제를 막았습니다.
36. 흰 바닥에서 실물 노란색 물체를 보정했습니다.
37. 물체 윤곽을 찾기 전에 집게 마커를 분리했습니다.
38. 특정 장면에 묶이지 않는 영상 기반 접촉 판단을 만들었습니다.
39. Uno 연결을 계속 유지하고 절대 바닥 평면 이동을 구현했습니다.
40. 전체 프로젝트 인수인계서를 작성했습니다.

## 패치 41–57 — 시뮬레이션, 학습, 바닥 기하

- 패치 41(첫 기록): 실물로 옮길 수 있는 원본 3차원 형상을 재현 가능하게
  정리했습니다.
- 패치 41(중복 기록): 다중 물체 바닥 집기와 ErrP용 “아님” 전환을
  추가했습니다.
- 패치 42(첫 기록): 실물 형상을 사용한 MuJoCo 로봇과 손목 카메라를
  만들었습니다.
- 패치 42(중복 기록): 실물로 측정한 바닥 자코비안과 깊이 정렬을
  바로잡았습니다.
- 패치 43(첫 기록): 환경을 무작위화한 근거리 정렬 정책을 학습했습니다.
- 패치 43(중복 기록): 먼저 보고 움직이는 바닥 사영 보정을 만들었습니다.
- 패치 44: 루트 문서와 Claude 인수인계서를 동기화했습니다.
- 패치 45: MuJoCo 관절 단위와 접촉 물리를 수정했습니다.
- 패치 46: 바닥 물체 전체 파지 정책을 학습하고 검증했습니다.
- 패치 47: 실물 카메라와 같은 입력을 만드는 그림자 어댑터를 추가했습니다.
- 패치 48: 전체 실물 실행 증거를 그대로 보존했습니다.
- 패치 49: 손목을 들 때 높이가 함께 변하는 문제를 보정하고 실물 수직
  집기 한 번을 검증했습니다.
- 패치 50: 실제 HOME부터 자동 탐색하고 엄격하게 물체 유지 여부를
  확인했습니다.
- 패치 51: 충돌 방지된 보기/접근과 탁자 접촉 보정을 만들었습니다.
- 패치 52: 여러 물체를 보고 선택하며 거부 기억을 유지했습니다.
- 패치 53: 반복 가능한 부드러운 탁자 접촉과 카메라 멈춤 복구를
  추가했습니다.
- 패치 54: 반사가 적은 위치에서 탁자 접촉을 더 깊게 검사했습니다.
- 패치 55: 마커가 사라졌을 때 접촉을 재생하고 변형 한계 안에서
  확인했습니다.
- 패치 57: 실물 1번 베이스 서보가 반응하지 않는 사실을 기록했습니다.

## 패치 58–72 — 3차원 작업실과 뇌파 공유 자율성

58. 사람이 편집할 수 있는 다중 물체 시뮬레이션과 PolyG-I ErrP 연결을
    추가했습니다.
59. 서버와 브라우저 시간이 달라 생긴 화면 초기화 오류를 없앴습니다.
60. 조잡한 2차원 모형을 실제 MuJoCo 3차원 작업실로 교체했습니다.
61. 고장 난 고정 베이스 조건을 3차원 작업실에도 똑같이 적용했습니다.
62. 시뮬레이션과 뇌파 그래프를 동시에 보고 8번 채널 ErrP 근거를
    확인하게 했습니다.
63. 보정 충돌 오류를 설명하고 오래 실행할 때 화면 메모리를 제한했습니다.
64. 연속 TAR 자율성 배분과 채널별 자동 축을 복구했습니다.
65. 짧은 움직임 잡음을 허용하고 각 채널 축을 완전히 독립시켰습니다.
66. 승인한 휴식 기준을 저장하고 보정 차단 이유를 표시했습니다.
67. TeleScan의 OVER와 ADC 끝값 포화를 분리하고 낮은 PGA 단계를
    복구했습니다.
68. 강한 사람 ErrP를 즉시 거부로 반영했습니다.
69. 일반 ErrP 기준을 50%로 고정하고 휴식 기준을 자동 복원했습니다.
70. 시뮬레이션에서 물체를 놓은 뒤에도 ErrP 판정을 계속했습니다.
71. 주기적으로 잘라 만든 가짜 사건 대신 비동기 슬라이딩 ErrP를
    적용했습니다.
72. 연속 ErrP 창 한 번만 기준을 넘겨도 반응하도록 했습니다.

## 패치 73–88 — 초음파 깊이와 HOME 자동 복귀

73. 손목 초음파 거리 센서를 추가했습니다.
74. 초음파 다중 반사를 거르고 실제 장착 위치를 모델에 반영했습니다.
75. 안전 검사에 실물 집게 끝 형상을 포함했습니다.
76. 실제 4번 모터 기준의 센서 좌표계를 확인했습니다.
77. 시간적으로 안정된 초음파 값을 요구하고 손목 크기를 맞췄습니다.
78. 서보 진동이 가라앉은 뒤 측정하게 했습니다.
79. 숫자상 그럴듯하지만 물리적으로 불가능한 초음파 보정을 거부했습니다.
80. 초음파를 부족했던 물체 깊이 축에만 사용했습니다.
81. 물체를 겨누기 위한 손목 회전 전후의 거리를 직접 비교하지 않게 했습니다.
82. 서보 떨림 범위 전체에서 초음파 접근 진행을 평가했습니다.
83. 실제 서보가 움직일 만큼 큰 접근 단계를 사용하고 집게 평면에서
    멈췄습니다.
84. 접근 정지 조건을 초음파 거리와 집게 끝 바닥 여유로 단순화했습니다.
85. 닫기 전에 바닥에서 여유를 만들고 들어 올린 뒤 유지 여부를
    확인했습니다.
86. 물체를 잡은 채 HOME으로 운반했습니다.
87. PWM 상태만 믿지 않고 하중이 걸린 HOME 명령을 실제로 다시 보냈습니다.
88. HOME에서 시작해 HOME으로 돌아오는 전 과정을 한 명령으로 만들었습니다.

## 패치 89–108 — 물체 추적과 파지 검증 강화

89. FastSAM이 놓친 실제 탁자 물체를 다른 영상 방법으로 복구했습니다.
90. 무개입 접근이 실물에서 재현된 전진 가지를 유지하게 했습니다.
91. 손목 조준과 팔 이동을 하나의 물리 움직임으로 함께 풀었습니다.
92. 보호된 바닥 접근을 자동 집기에 연결했습니다.
93. 사용하지 않을 자세의 점수를 계산하기 전에 바닥 정지 조건을
    우선했습니다.
94. 닫기 전 높이를 실측 서보 한 단계에 맞춰 조절했습니다.
95. 최종 닫기 때 잠근 전체 물체를 측정했습니다.
96. 닫힌 집게 통로 안에 물체가 유지되는지 확인했습니다.
97. 하중이 걸린 집게 때문에 HOME 운반이 멈추는 문제를 제거했습니다.
98. 물체를 든 유지 각도를 실물에서 복구된 158°로 정했습니다.
99. 실물 다중 물체 선택에 외부 거부 신호를 연결했습니다.
100. 선택 사항인 결정 신호 파일이 없어도 실행되게 했습니다.
101. 실시간 집게 중심에서 접근 축을 계산했습니다.
102. 손목 카메라로 물체를 든 HOME 복귀를 검증했습니다.
103. 마지막으로 물체가 보였던 안전 자세부터 접근했습니다.
104. 실시간 접근 중 물체 전체 마스크를 유지했습니다.
105. 카메라 화면 위쪽에서는 보정된 열린 집게 형상을 사용했습니다.
106. 단계별 관찰 사이에서도 같은 물체 ID를 유지했습니다.
107. 가려진 마스크를 보완할 때 실물 물체 내부를 벗어나지 않게 했습니다.
108. 닫기 전에 집게 증거가 안정될 때까지 기다렸습니다.

## 패치 109–124 — 실시간 접근과 Windows 인수인계

109. 실시간 입출력 경로에서 움직인 뒤 멈춰 기다리는 구조를 제거했습니다.
110. 시간적으로 연속된 추적과 자코비안 접근을 적용했습니다.
111. AI 모델 첫 실행 지연 뒤 물체를 다시 찾게 했습니다.
112. 비동기 움직임 완료와 초음파 측정을 분리했습니다.
113. 손목 카메라 초기 대상 선택의 고정 시간 제한을 없앴습니다.
114. 물체 내부 색상 기준점으로 전체 물체를 계속 추적했습니다.
115. 로봇 자신의 화면 하단 집게 색 영역을 대상에서 제외했습니다.
116. 과노출된 손목 카메라에서도 색 물체를 복구했습니다.
117. 실제 장착 카메라의 위아래 제어 방향을 바로잡았습니다.
118. 물체 가까이에서 제한된 손목 각도 보정을 허용했습니다.
119. 프로그램을 다시 시작해도 현재 실물 자세에서 연속 접근했습니다.
120. 사용자가 지정한 바닥 정지 시 집기 분기를 구현했습니다.
121. HOME 화면의 방이나 사람을 처음 목표로 선택하지 않게 했습니다.
122. 접근 정지 즉시 집고 물체를 든 채 HOME으로 가게 했습니다.
123. Windows 이식 전에 최종 macOS 실물 보정을 태그로 보존했습니다.
124. 새 Windows PC에서 설치·카메라 확인·진단·자동 실행이 가능한 독립
     배포판을 만들고 FastSAM 모델을 함께 넣었습니다.

## 패치 125 — 모든 사용자 문서와 Windows 안내를 쉬운 한국어로 통일

### 목적

코딩 경험이 적은 친구가 새 Windows PC에서 저장소를 받아 설치하고,
카메라와 Arduino 연결 문제를 스스로 확인하며, 자동 탐색·접근·집기·HOME
복귀를 실행할 수 있게 합니다.

### 변경 내용

- 루트 `README.md`를 한국어 초보자 안내서로 다시 작성했습니다.
- Windows 빠른 시작, 상세 실행서, BAT/PowerShell 설치 창, Python 실행
  진행 문구와 오류 해결 조언을 한국어로 통일했습니다.
- 한국어가 Windows 명령 프롬프트에서 깨지지 않도록 BAT 파일은 UTF-8
  코드 페이지를 사용하고 PowerShell 파일에는 UTF-8 BOM을 적용했습니다.
- 카메라 권한, 카메라 점유, COM 포트, CH340, Python/winget/pip,
  방전 배터리, 공통 GND, HOME 불일치, 수리 후 보정 변화에 대한 자세한
  문제 해결 절차를 추가했습니다.
- 자동 제어기의 내부 영문 상태값은 그대로 유지하되 Windows 실행 창에는
  `물체를 찾지 못함`, `추적을 잃음`, `HOME 복귀 완료`처럼 쉬운 한국어
  설명을 함께 표시합니다.
- `QHAND.md`, 대시보드 설명서, 시뮬레이션 설명서, 학습 보고서, 실물
  검증 기록을 한국어로 바꿨습니다.
- 내부 API 키, 파일명, 명령어, 모델명처럼 코드 호환에 필요한 식별자는
  번역하지 않았습니다.
- 과거 검증 기록은 날짜 당시의 실패와 한계를 그대로 유지했습니다.
- 현재 펌웨어에서 6번 HOME이 180°인데 구형 파이프라인 검사가 170°를
  기대하던 항목을 실제 기준에 맞췄습니다.
- 현재 바닥 대기 어깨 기준이 110°인데 오래된 검사가 124°를 기대하던
  항목도 최신 `config.py` 기준으로 동기화했습니다.
- 같은 바닥 곡선에서 팔꿈치 86°일 때 보상 어깨값도 현재 계산 결과인
  112°로 검사하도록 바로잡았습니다.
- 번역 중 함께 검사한 `vision_segment.py`에서 사용하지 않던 지역 변수
  하나를 제거해 Ruff 검사를 깨끗하게 통과하게 했습니다.

## 패치 126 — Windows CMD 한글 깨짐과 펌웨어 경로 오류 수정

- 모든 `.bat` 파일을 ASCII 문자만 포함하는 짧은 실행기로 변경하여,
  구형 Windows CMD가 UTF-8 한글을 명령으로 잘못 해석하지 않게 했습니다.
- 한국어 안내와 경로 처리는 UTF-8 BOM이 있는 PowerShell 실행기로
  이동했습니다.
- `OPEN_FIRMWARE.bat`은 공백·한글이 포함된 경로에서도 실제 스케치 파일의
  존재를 확인한 뒤 Arduino IDE를 실행합니다.
- 전체 ZIP이 아닌 `windows_release` 폴더만 복사한 경우 확인한 경로와
  해결 방법을 한국어로 표시합니다.
- 카메라 확인, 연결 진단, 자동 실행도 동일한 PowerShell 실행기를 사용해
  한글 출력과 저장소 경로 처리를 통일했습니다.
- Windows 배치 파일은 Git에서 CRLF 줄바꿈으로 배포되도록 고정했습니다.

### 확인 항목

- Windows 배포판 단위 검사
- Python 문법 검사
- Ruff 정적 검사
- FastSAM 모델과 필수 패키지를 불러오는 설치 검사
- 저장소가 추적하는 사용자 문서의 한국어 여부 점검

## 패치 127 — 4·6번 고장 이후 2자유도 전용 제어 모드 추가

### 목적

기존 전체 자유도 작업물을 보존하면서, 현재 실제로 사용할 수 있는 2번
어깨·3번 팔꿈치·5번 집게만으로 로봇팔을 제어합니다. 4번 이후 손목은
3번 링크에 고정된 강체로 계산합니다.

### 변경 내용

- 기존 펌웨어와 제어 코드는 수정하지 않고 `arm_controller_reduced`
  펌웨어를 별도로 추가했습니다.
- 축소 펌웨어는 2·3·5번 핀만 `Servo.attach()`하며 1·4·6번에는 PWM을
  출력하지 않습니다.
- 고정 손목을 포함한 2관절 순기구학, 2×2 카메라 자코비안, 중간 경로 충돌
  검사, 바닥 여유 검사를 추가했습니다.
- 고장 축 값이 바뀐 명령은 노트북과 펌웨어 양쪽에서 거부합니다.
- Uno 재연결 리셋을 피하는 별도 축소 세션과 2·3·5번 전용 JOG를
  추가했습니다.
- 카메라 추적과 초음파 거리를 사용해 두 관절을 연속 보정하고, 접근 후
  집게를 닫아 짧게 들어 올린 뒤 닫힘 180°를 유지한 채 축소 HOME으로
  복귀하는 별도 자동 제어기를 추가했습니다.
- 고정 손목의 실제 각도는 `FIXED_WRIST_GEOMETRY_DEG` 한 값으로 보정하게
  만들었습니다.
- 전용 펌웨어 컴파일·업로드 도구와 한국어 실행 문서를 추가했습니다.

### 확인 항목

- 축소 자유도 단위 검사 9개
- 기존 안전·세션·실시간 제어 회귀 검사
- Python 문법 검사와 Ruff 정적 검사
- Arduino Uno 대상 축소 펌웨어 컴파일
- 1·4·6번 출력 핀이 펌웨어 attach 대상에 없는지 정적 검사

## 패치 128 — 4번 손목 고정 전용 시뮬레이션과 정책 학습

### 목적

기존 손목 가동 시뮬레이션과 학습 결과를 보존하면서, 현재 실물처럼
2·3번 관절과 5번 집게만 사용할 수 있는 별도 시뮬레이션·학습·실물 전이
경로를 추가했습니다.

### 변경 내용

- MuJoCo 관절 트리와 액추에이터에서 1·4·6번을 완전히 제거한 축소 모델을
  추가했습니다. 4·6번은 고정 강체 변환으로만 존재합니다.
- 실제 STL 외형을 유지하고 2·3번 관절, 카메라, 초음파, 손끝, 집게의
  좌표가 실물용 축소 순기구학과 일치하도록 만들었습니다.
- 굽은 어깨 링크의 관절 중심 오프셋을 `upper_dx`, `upper_dz`로 명시해
  이후 실측값을 반영할 수 있게 했습니다.
- 실물에서 얻을 수 있는 16개 관측만 사용하는 탐색→접근→닫기→리프트→
  HOME 복귀 환경과 8개 상위 행동 정책을 추가했습니다.
- DAgger 방식으로 190,000개 상태를 학습해 별도 TorchScript 모델과
  SHA-256, 재현용 지표를 저장했습니다.
- 실물 관측을 동일 입력으로 바꾸는 어댑터와 `--learned-policy` 실행 옵션을
  추가했습니다. 실제 각도 증분과 최종 허용은 기존 자코비안·충돌 검사가
  계속 담당합니다.
- 학습된 전체 과정을 3차원 창에서 확인하는 축소 전용 데모를 추가했습니다.
- 자유 물체 접촉 평가에서 상자 104/104, 원기둥 97/97, 구 42/99,
  전체 243/300의 실제 들어 올리기를 확인했습니다. 구형 실패는 지원 한계로
  문서화했습니다.

### 확인 항목

- MuJoCo 액추에이터가 2·3번과 좌우 집게 네 개뿐인지 검사
- 시뮬레이션 도구 중심·손끝과 실물 축소 순기구학 좌표 일치 검사
- 무작위 환경 전문가 및 학습 정책 닫힌 고리 검사
- 실물 관측 어댑터와 학습 입력 규격 일치 검사
- 300회 자유 물체 접촉·리프트 평가
- Python 문법, Ruff 정적 검사, 기존 축소 안전·세션 회귀 검사

## 패치 129 — 전방 통풍구 본체 방향 수정과 사고 자세 영구 차단

### 사고 원인

실물 사진에서 하부 통풍구 본체는 팔이 뻗는 `+X` 방향이지만, 기존
MuJoCo는 출력판의 `Alt_Kasa.stl` 좌표를 조립 좌표로 잘못 사용해 본체를
반대 `-X` 방향에 표시했습니다. 실물 안전 모델도 하부 몸체를 방향성 없는
높이 120mm 원통으로 근사해 2번 95°·3번 165° 자세를 안전하다고 잘못
판정했습니다. 실제 이동에서 고정 손목이 본체 전면 상단에 걸렸습니다.

### 변경 내용

- 실행 중이던 축소 세션을 즉시 종료해 추가 명령을 차단했습니다.
- 실물 인터록의 하부 몸체를 전방 방향성 3D 상자로 교체했습니다.
- MuJoCo 전체/축소 모델의 베이스 메시를 전방으로 돌리고 실제 통풍구 덮개
  `Alt_Kapak.stl`을 추가했습니다.
- 시뮬레이션 충돌 상자에 실물과 같은 12mm 여유를 포함하고, 링크 충돌
  반경도 실물 안전 모델과 일치시켰습니다.
- 로봇 링크와 베이스 본체가 MuJoCo에서 실제 self-contact를 만들도록 충돌
  비트를 분리했습니다.
- 사고 자세와 HOME에서 사고 자세로 가는 중간 경로를 실물 인터록이
  `base-housing`으로 거부하는 회귀검사를 추가했습니다.
- 같은 사고 자세가 MuJoCo에서도 전방 본체와 접촉하는 검사를 추가했습니다.
- 기존 학습 후보 중 전방 본체 여유가 작은 자세를 모두 폐기하고, 12mm
  모델 여유 이후에도 전체 파지·리프트 경로에 약 10mm 이상 남는 자세만
  사용해 정책을 다시 학습했습니다.
- 수정된 190,000개 상태 학습은 무작위 작업 10,000/10,000을 완료했습니다.
  실제 접촉 평가는 상자 104/104, 원기둥 75/97, 구 0/99, 전체
  179/300이었습니다. 안전 영역을 바로잡은 뒤 낮아진 결과를 그대로
  기록하고 현재 1차 지원 대상을 상자형으로 제한했습니다.

### 확인 항목

- 사고 자세 실물 인터록 거부
- 사고 자세 MuJoCo self-contact
- 베이스 메시와 충돌 상자의 `+X` 방향 검사
- 모든 새 학습 자세의 HOME·파지·리프트 전 구간 여유 검사
- 축소 정책 재학습과 자유 물체 접촉 평가
- 전체 안전·시뮬레이션 회귀 검사

## 패치 130 — Windows 뇌파·시뮬레이션·실물 통합 운영실

### 목적

친구가 Windows에서 여러 Python 명령을 직접 입력하지 않고 프로그램 하나를
열어 PolyG-I, 3D 시뮬레이션, 손목 카메라, Arduino, 다중 물체 자동 파지를
버튼으로 관리하게 합니다. 수리 완료 예정 구성을 반영해 실물 기본 모드는
1·6번 고정, 2·3·4번 관절과 5번 집게 사용으로 정했습니다. 기존 macOS와
2자유도 고장 대응 코드는 삭제하지 않고 그대로 보존했습니다.

### 변경 내용

- `START_CONTROL_CENTER.bat` 하나로 로컬 API, 전용 React 화면, Windows
  WebView2 창을 함께 시작하는 실행기를 추가했습니다. 전용 창을 열 수
  없으면 기본 브라우저로 자동 전환합니다.
- 첫 화면에 PolyG-I 측정, 카메라, Uno의 3단 연결 절차와 상태 표시를
  배치했습니다.
- 기존 8채널 EEG 파형, CSV 기록, 저장 휴식 기준, CH8 ErrP, CH1–4/CH8
  TAR 화면과 MuJoCo 다중 물체 작업실을 같은 프로그램의 탭으로 연결했습니다.
- FastSAM 다중 후보를 번호로 표시하고 사용자가 후보를 고른 뒤 자동
  접근·파지·HOME 복귀를 시작하게 했습니다.
- 실물 자동 제어는 기존에 검증한 실시간 카메라 추적, 2·3·4축 자코비안,
  초음파 정지, 즉시 닫기 흐름을 재사용합니다.
- 후보 제시 단계의 수동/ErrP 거부는 거부 기억에 추가하고 다음 후보로
  전환합니다. 모든 후보가 거부되면 기억을 초기화합니다. 이미 실물 이동이
  시작된 뒤의 거부는 새 방향을 추측하지 않고 현재 이동을 정지합니다.
- TAR 로봇 가중치가 높으면 후보 검토 시간을 줄이고, 인간 가중치가 높으면
  ErrP를 낼 시간을 늘리도록 연결했습니다.
- 2·3·4번과 집게 수동 슬라이더, HOME 확인 체크, 연결 설정 저장, 한국어
  실행 기록, 움직임 없는 진단 화면을 추가했습니다.
- 펌웨어에 `X` 즉시 정지 명령을 추가했습니다. 이 명령은 남은 보간 이동을
  취소하고 현재 명령 위치를 유지하며, 실제 위험에서는 외부 서보 전원을
  별도로 분리해야 합니다.
- Windows 설치기가 Python뿐 아니라 Node.js, HID, SciPy, MuJoCo, WebView와
  GUI 빌드까지 한 번에 준비하도록 확장했습니다.
- Windows 빠른 시작과 문제 해결 문서를 통합 운영실 기준의 한국어로
  다시 정리했습니다.

### 확인 항목

- Windows 실행기·설정·긴급정지·포트 선택 단위 검사
- 기존 2·3·4축 실시간 접근 및 축소 자유도 회귀 검사 38개
- React 배포 빌드와 서버 렌더 검사
- 통합 API, EEG 상태 API, MuJoCo 상태 API 로컬 기동 검사
- Python 문법 검사와 배치 파일 CRLF/PowerShell UTF-8 BOM 검사

### 남은 실물 확인

- 친구의 Windows PC, 수리된 2·3·4번 모터, 외부 배터리, 실제 PolyG-I를
  합친 전체 조합은 해당 PC에서 최종 확인해야 합니다.
- 3D 시뮬레이션은 바구니 배송 뒤 늦은 ErrP까지 자동 회수하지만, 실물은
  이동 중 ErrP에서 안전 정지합니다. 실물 원위치 자동 반환은 수리된 기구의
  역경로를 별도로 검증한 뒤 활성화해야 합니다.

## 패치 131 — Windows 단일 설치 앱과 실제 GitHub 릴리스 자동화

### 문제

`windows-control-center-v1`은 Git 태그와 소스 코드만 원격에 있었고 GitHub
Releases에는 실행 가능한 새 자산이 없었습니다. 사용자는 여전히 Python,
Node.js와 여러 `.bat` 파일을 직접 다뤄야 했으므로 “앱 하나를 열어 GUI에서
모든 설정과 실험을 진행한다”는 요구를 충족하지 못했습니다.

### 변경 내용

- 별도 Node.js 서버 없이 Python 앱이 React 정적 화면을 직접 제공하도록
  Windows 전용 Vite 빌드를 추가했습니다.
- GUI, Python 3.11, FastSAM/PyTorch, EEG, MuJoCo, OpenCV, HID, 시리얼
  런타임을 PyInstaller `onedir` 앱으로 묶는 규격을 추가했습니다.
- 묶인 카메라 프로세스가 잘못 자기 자신을 다시 여는 문제를 막기 위해
  전용 `--camera-worker` 내부 실행 모드를 추가했습니다.
- Inno Setup으로 일반 사용자가 받는 파일을
  `brainToArm-Windows-Setup-v2.0.0.exe` 하나로 만들었습니다. 사용자 전용
  설치, 시작 메뉴/바탕화면 아이콘, 설치 후 자동 실행을 포함합니다.
- 첫 실행부터 통합 운영실이 열리고 PolyG-I, 카메라, Uno, 시뮬레이션,
  다중 후보, ErrP/TAR와 설정을 GUI에서 다루도록 했습니다.
- GUI에서 현재 Arduino 펌웨어를 바로 열 수 있는 버튼을 추가했습니다.
- 앱 시작 실패를 한국어 메시지 창과
  `%LOCALAPPDATA%\brainToArm\logs\startup-error.txt`에 남깁니다.
- Windows x64 GitHub Actions가 실제 `.exe`를 빌드하고 자체 검사, 내장 GUI
  HTTP 검사, SHA256 생성 후 GitHub Release를 생성하도록 했습니다.
- 설치·실행 문서를 단일 설치 앱 기준으로 다시 작성하고 구형 배치 파일은
  개발자용으로만 구분했습니다.

### 확인 항목

- Windows 내장 React GUI production 빌드
- 기존 Windows 단위 검사 17개
- EEG, 실시간 시각 서보, MuJoCo Studio 핵심 모듈 가져오기
- 내장 HTML과 통합 상태 API의 동일 포트 기동 검사
- GitHub Windows x64 패키지 자체 검사와 설치 파일 생성 자동화

## 패치 132 — Windows `_internal` 펌웨어 누락 수정과 시뮬레이터 실기동 검사

### 원인

v2.0.0은 펌웨어를 설치 폴더 바깥 계층에 복사했지만, PyInstaller가 묶은
`config.py`는 `_internal\firmware\arm_controller\home_pose.h`에서 여섯 개
HOME 상수를 읽었습니다. GUI와 상태 API는 이 경로를 사용하지 않아 빌드
검사를 통과했지만, MuJoCo Studio를 처음 열 때 설정 모듈이 해당 파일을
읽으며 실패했습니다.

### 변경 내용

- PyInstaller 데이터에 `firmware/` 전체를 추가해 `_internal` 안에 반드시
  포함되게 했습니다.
- 독립 실행 앱 자체 검사가 `_internal`의 `home_pose.h` 존재를 직접 검사하게
  했습니다.
- Windows 패키지 검사에서 `/api/simulation/start`를 실제 호출하고 640×360
  MuJoCo JPEG 프레임이 10KB 이상 생성되는지 확인한 뒤 시뮬레이션을
  종료하게 했습니다.
- v2.0.1 설치 파일과 릴리스 문서를 수정 버전으로 갱신했습니다.

### 재검증 기준

- Windows x64 독립 실행 앱 자체 검사
- 내장 GUI와 실제 상태 API 기동
- MuJoCo Studio 시작
- overview 카메라 JPEG 렌더
- 시뮬레이션 정상 종료
