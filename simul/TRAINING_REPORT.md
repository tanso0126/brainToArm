# 바닥 물체 전체 파지 정책 v1 학습 보고서

## 제공 범위

`models/full_task_policy_v1.ts`는 전체 작업용 상위 정책입니다. 다음
결정 순서를 담당합니다.

```text
탐색 → 정렬 → 하강 → 집게 닫기 → 들어 올리기 → 복구
```

결정론적 안전 코드는 각 상위 행동을 제한된 `floor_pose` 명령으로
변환하고, 최신 카메라 근거를 요구합니다. 이전
`alignment_policy_v1.ts`는 결과 재현용으로만 남겨 두었으며 현재
통합에는 전체 작업 정책을 사용합니다.

모델 파일 SHA-256:

```text
b4a5cf2b976b7571bf38b2b7e96d30d5159d00186e12d93569fe91cdfa4772b7
```

## 실물로 옮길 수 있는 입력 경계

신경망 입력은 실물 Mac 제어 과정에서도 얻을 수 있는 15개 값입니다.

1. 영상 품질, 물체 보임 여부, 두 집게 마커 보임 여부, 물체 추적 연속성
2. 물체와 집게 사이의 영상상 깊이 오차 및 중심선 오차
3. 정규화한 집게 열림 정도와 일관된 들어 올리기 움직임
4. 이미 명령한 6개 서보 각도와 이전 상위 행동

물체의 실제 좌표, 시뮬레이션 깊이, 접촉, 모양, 크기, 목표 팔꿈치 각도,
보상, MuJoCo 내부 상태는 모델 입력에 들어가지 않습니다. 깊이 반응은
합성 카메라 값을 그대로 믿지 않고 실물에서 측정한
`-12.9픽셀/도` 자코비안을 중심으로 무작위화했습니다.

`laptop/full_task_adapter.py`는 카메라나 시리얼 포트를 직접 열지 않고
실물 관측과 명령 자세에서 같은 15개 입력을 만듭니다.

## 학습 결과

DAgger 방식의 행동 복제 학습으로 전문가 및 잡음 상태 120,000개와,
정책 교란 상태 40,000개씩 세 차례를 수집해 총 240,000개 상태를
사용했습니다.

- 신경망 구조: `15 → 192 → 128 → 64 → 8`
- 별도 검증 자료 행동 정확도: 98.2882%
- 무작위 닫힌 고리 성공: 2,000회 중 95.55%
- 두 프레임 카메라/자세 안전 조건:
  10,000회 중 9,998회 완료(99.98%, 평균 11.4678 상위 프레임)
- 결정론적 실험: 1,000회 중 1,000회 완료

학습 무작위화는 실제 손목 카메라처럼 잡기 자세 대부분에서 물체 마스크가
가려지도록 합니다. 대기 높이에서 정렬을 검증해 대상을 잠갔다면 가려진
동안에도 연속 대상으로 유지할 수 있지만, 사전 잠금 없이 마스크가
사라지면 집게 닫기를 허용하지 않습니다.

## 독립 접촉 물리 평가

성공하려면 다음 조건을 모두 만족해야 합니다.

- 물체가 최소 3mm 올라감
- 물체 아랫면이 바닥에서 떨어짐
- 물체와 바닥 접촉이 사라짐
- 물체 XY 위치가 도구를 따라감

평가는 제한된 서보 궤적, 상자·원기둥·구 물체, 반크기 14–22mm,
무작위 도달 깊이, 가로 오차, 센서 끊김, 최대 두 번의 카메라식 재탐색을
사용했습니다.

서로 겹치지 않는 최종 시드 2,000개의 결과:

| 평가 항목 | 결과 |
|---|---:|
| 상태기계 작업 완료 | 2,000/2,000 (100%) |
| 28–44mm 전체 물체의 물리적 들어 올리기 | 1,959/2,000 (97.95%) |
| 첫 시도 물리적 들어 올리기 | 1,952/2,000 (97.6%) |
| 폭 40mm 이하 일반 범위 | 1,488/1,488 (100%) |
| 폭 40–44mm 한계 크기 스트레스 실험 | 471/512 (91.9922%) |

남은 실패는 모두 크기가 큰 한계 물체였습니다. 들어 올리기 기준을 낮춰
성공으로 바꾸지 않고 실패로 기록했습니다. 따라서 현재 시뮬레이션의
정상 집게 용량 범위는 40mm 이하입니다. 실물 집게를 측정하기 전에는 이를
실물 한계로 주장하지 않습니다.

## 안전 및 현실과의 경계

위 비율은 시뮬레이션 결과이며 실물 로봇팔 성공률이 아닙니다. 보호 조건이
있는 모델은 2026-07-27 실물 실험에서 상위 행동 조건으로 사용됐고, 당시
고정 도달 제어기는 집게 접촉이 들어 올린 뒤에도 남은 실물 성공 한 번을
만들었습니다. 자세한 내용은
[실물 검증 기록](../docs/PHYSICAL_GRASP_VALIDATION.md)에 있습니다.

학습 결과가 실물의 마커, 정렬, 집게 접촉 확인을 건너뛰지는 않습니다.

## 결과 재현

```bash
python3 simul/prepare_assets.py --extract
python3 -m unittest simul.test_mujoco_robot simul.test_full_task -v
python3 simul/train_full_task.py
python3 simul/evaluate_full_task_physics.py --policy simul/models/full_task_policy_v1.ts --episodes 2000 --seed 20260729
PYTHONPATH=laptop python3 laptop/test_pipeline.py
```

학습 출력은 Git에서 제외된 `simul/generated/`에 저장됩니다. 검토한 모델,
정확한 평가 수치, 재현에 필요한 소스만 저장소에서 추적합니다.

---

# 4번 고정 축소 자유도 정책 v1

## 모델 계약

`models/reduced_dof_policy_v1.ts`는 현재 고장 상태를 별도 모델로 학습한
정책입니다. 1·4·6번은 고정이며, 제어 가능한 출력은 2번 어깨, 3번 팔꿈치,
5번 집게뿐입니다. MuJoCo에도 4번과 6번 관절/액추에이터가 존재하지
않으므로 학습 정책이 사용할 수 없는 자유도를 몰래 이용할 수 없습니다.

정책 입력은 실물에서 얻을 수 있는 16개 값이고 출력은 다음 8개 상위
행동입니다.

```text
대기 / 다음 탐색 / 접근 / 닫기 / 들어 올리기 / HOME 복귀 / 복구 / 완료
```

모터 각도 증분은 신경망이 직접 출력하지 않습니다. 실물에서는 기존 2×2
카메라 자코비안, 초음파 거리, 계산상 바닥 여유, 중간 경로 충돌 검사가
실제 2·3번 명령을 결정합니다.

모델 SHA-256:

```text
07ae6cbd8a5c575c16df43544c67eb88eb5d3c7eb6a22a235155d19783bc3fbd
```

## 학습 결과

DAgger 방식 모방학습으로 총 190,000개 상태를 사용했습니다.

- 신경망 구조: `16 → 128 → 96 → 48 → 8`
- 별도 검증 행동 정확도: 99.9825%
- 센서 누락·영상 오차·초음파 오차를 넣은 무작위 작업:
  10,000/10,000 완료, 평균 18.9415 상위 프레임
- 결정론적 작업: 1,000/1,000 완료, 평균 16.72 상위 프레임

## 독립 접촉 물리 평가

300개 자유 물체를 실제 MuJoCo 접촉으로 평가했습니다. 상태기계의
`holding=True`만으로는 성공할 수 없고, 물체가 30mm 이상 올라가고 바닥
접촉을 잃으며 손을 따라가야 합니다.

| 물체 형상 | 성공 |
|---|---:|
| 상자 | 104/104 (100%) |
| 원기둥 | 75/97 (77.32%) |
| 구 | 0/99 (0%) |
| 전체 | 179/300 (59.67%) |

전방 본체 충돌 영역과 추가 경로 여유를 반영하자 손끝을 이전만큼 낮출 수
없어 전체 접촉 성공률이 낮아졌습니다. 상자는 모두 성공했지만 작은
원기둥은 일부 놓쳤고 구는 평행 집게에서 전부 굴러 빠졌습니다. 이 결과를
이전의 잘못된 81%로 유지하거나 성공 기준을 낮추지 않았습니다. 현재
정책을 실물에 적용할 때는 상자형 물체만 1차 지원 대상으로 보고,
원기둥·구형 또는 낮은 물체는 손목/집게 수리나 패드 변경 전까지 지원한다고
주장하지 않습니다.

위 결과는 2026-08-02 사진으로 전방 통풍구 본체 방향을 바로잡고, 사고
자세를 차단하며, 12mm 모델 여유 이후에도 약 10mm의 경로 여유가 남는
파지 자세만 사용해 정책을 다시 학습한 결과입니다.

## 아직 실측해야 하는 값

시뮬레이션과 실물용 축소 순기구학은 여러 자세에서 손끝 좌표가 일치합니다.
그러나 다음 두 값은 아직 현재 조립 상태를 직접 재측정하지 않았습니다.

- 3번 링크에 고정된 4번 손목의 실제 상대 각도
- 굽은 어깨 부품 양끝의 실제 관절 중심 간 `(dx, dz)`

따라서 위 수치는 시뮬레이션 평가 결과이지 실물 성공률이 아닙니다. 자동
실물 실행 전 `FIXED_WRIST_GEOMETRY_DEG`와 관절 중심 오프셋을 측정하고,
JOG 저속 확인 및 무부하 접근 검사를 거쳐야 합니다.

## 재현 명령

```bash
python3 -m simul.train_reduced_dof --device cpu
python3 -m simul.evaluate_reduced_dof_physics --episodes 300
python3 -m simul.reduced_dof_demo
python3 -m unittest simul.test_reduced_dof_sim -v
```
